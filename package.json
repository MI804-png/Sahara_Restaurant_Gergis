{
	"name": "sahara-restaurant-workspace",
	"private": true,
	"scripts": {
		"dev": "npm --prefix site run dev",
		"build": "npm --prefix site run build",
		"serve": "node temp-server.cjs",
		"start": "node temp-server.cjs"
	}
}
